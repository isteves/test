from setuptools import setup, find_packages

setup(
    name='testpkg',
    version='0.0.2',
    description='blah',
    author='auth',
    packages=find_packages(),
    license='unlicense',
    install_requires=[]
)
